generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql" // ganti mysql jika perlu
  url      = env("DATABASE_URL")
}

model User {
  id        Int      @id @default(autoincrement())
  name      String
  email     String   @unique
  password  String
  createdAt DateTime @default(now())
}
